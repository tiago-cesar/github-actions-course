name: "01 - Building Blocks"
on:
  - push

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: "say hello"
        run: echo "Hello World!"
  failed-step:
    runs-on: ubuntu-latest
    steps:
      - name: "Failed Step"
        run: |
          echo "I will fail"
          exit 1
      - name: "Say goodbye"
        run: echo "Goodbye!"